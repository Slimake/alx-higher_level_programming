#!/usr/bin/python3
def number_keys(a_dictionary):
    """Returns the number of keys in a dictionary

        Args:
            a_dictionary: dict

        Returns:
            The return value. int
        """
    # Using the length function, we can get the length of a_dictionary
    return len(a_dictionary)
